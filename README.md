# Setup Steps

1. Install conda and configure environment

``` bash
conda create -n simple python=3.12 -y && conda activate simple
```
2. Activiate environment
``` bash
conda activate simple
```
3. Install node and ollamaUI
4. make directory and create main.py file
5. run main file
``` bash
pip instll ollama
python main.py
```
